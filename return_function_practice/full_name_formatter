#Write a function that takes a first name and last name and returns them as a single full name.

def full_name(first_name: str, last_name: str) -> str:
    return f"{first_name} {last_name}"

print(full_name("David", "Freedman") == "David Freedman"

